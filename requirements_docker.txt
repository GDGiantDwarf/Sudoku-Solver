# Docker subset — ultralytics excluded (large dependency, not needed in Docker solve mode)
# Benchmark and YOLO detection are unavailable in Docker.
selenium==4.18.1
opencv-python==4.11.0.86
numpy==2.1.3
pytesseract==0.3.13
Pillow==11.0.0
py-sudoku==1.0.3