# Core browser automation
selenium==4.18.1
# Grid detection and image preprocessing
# 4.11.0.86 is the first release with Python 3.13 wheels built against NumPy 2.x
opencv-python==4.11.0.86
numpy==2.1.3
# Input automation (keyboard/mouse entry)
pyautogui==0.9.54

# NOTE: pytesseract is a Python wrapper — Tesseract itself must also be installed
# separately on the system (see README for instructions).
pytesseract==0.3.13
# Required by pytesseract for image handling
Pillow==11.0.0
# Sudoku solving — constraint-propagation engine
py-sudoku==1.0.3
#ultralytics - YOLOv8 for digit recognition 
ultralytics==8.3.223
 